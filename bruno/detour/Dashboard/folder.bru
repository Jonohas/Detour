meta {
  name: Dashboard
  seq: 6
}

auth {
  mode: none
}

docs {
  Authenticated by a read-only dashboard key, not a rider session. Issue one with ApiKeys > Issue a read-only dashboard key and put it in API_KEY.
}
