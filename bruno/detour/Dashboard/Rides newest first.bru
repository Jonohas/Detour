meta {
  name: Rides, newest first
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/dashboard/rides?limit={{limit}}
  body: none
  auth: none
}

params:query {
  limit: {{limit}}
}

headers {
  X-Api-Key: {{API_KEY}}
}

assert {
  res.status: eq 200
}

docs {
  Responses:
  - 200: OK
}
