<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Android Auto gates the NAVIGATION category behind this permission: a
         car app service declaring that category is not listed on the head unit
         without it, and NavigationTemplate throws when used. ACCESS_SURFACE is
         what lets CarMapRenderer draw onto the car's raw Surface. -->
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- CircleNotifyService holds the relay socket open for circle arrival
         notifications - remoteMessaging, not location: it never reads or
         shares this device's own position, only listens. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Push-to-talk capture while a convoy is joined. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Lets ConvoyLiveService set MODE_IN_COMMUNICATION for echo
         cancellation during push-to-talk; without it AudioManager.setMode()
         is silently ignored. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Read bonded devices + Classic ACL connect/disconnect, to auto-pick the
         vehicle from a connected Cardo/infotainment. Never scans, so no location. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Advertise the phone as a BLE peripheral for an external display
         (e.g. a handlebar-mounted screen) to connect to. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <application
        android:name=".DetourApplication"
        android:label="Detour"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Detour"
        android:supportsRtl="true"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Where the realm sends the browser back after a sign-in, carrying
                 the authorization code (see auth/Oidc.kt). A private-use scheme
                 because that is what a native app can claim without hosting
                 anything, and it is what the realm has registered for this
                 client. singleTop above keeps the redirect from stacking a
                 second copy of the app on top of the running one.

                 Password reset had a filter here too. The realm owns that now:
                 its mail links into a browser, the rider signs in afterwards
                 like any other time, and the app never handles a reset code. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="detour" android:host="auth" android:path="/callback" />
            </intent-filter>
        </activity>

        <service
            android:name=".tracking.TripTrackingService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Keeps the convoy WebSocket alive and hosts push-to-talk capture
             while a convoy is joined; runs independently of trip tracking
             (a convoy can be joined without a trip running). -->
        <service
            android:name=".convoy.ConvoyLiveService"
            android:foregroundServiceType="location|microphone"
            android:exported="false" />

        <!-- Keeps the relay socket connected for circle arrival/departure
             notifications, independent of trip tracking and of any convoy -
             see CircleNotifyService's doc for why it isn't just folded into
             TripTrackingService. -->
        <service
            android:name=".notif.CircleNotifyService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false" />

        <!-- Android Auto: "Spin" from the car screen, hand off to the phone's
             default nav app. Same APK/process as the phone UI — no separate
             module, unlike the genuinely standalone :wear app. -->
        <service
            android:name=".car.DetourCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>

        <service
            android:name=".media.MediaListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Hands a single exported GPX to the app the user picks. Not
             exported and no granted paths beyond the share cache — the trace
             store itself never becomes readable to another app. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".tracking.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
